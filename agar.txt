public void makeAgar(){
        //Random randPos   = new Random();
        //AnimatedSprite aniBubbles;
        //Sprite aniBubbles;
        Sprite aniBubbles[] = new Sprite[70];
        
        //Sprite aniBubbles[] = new Sprite aniBubbles[30];
        for (int i = 0; i < AMOUNT_BUBBLES; i++){
            BufferedImage image = new  BufferedImage(WIDTH_HERO, HEIGHT_HERO, BufferedImage.TYPE_INT_ARGB);
            Graphics2D graphics = (Graphics2D) image.getGraphics();
            graphics.setColor(Color.yellow);
            graphics.fillOval(0, 0, 20, 20);
            
            aniBubbles[i] = new Sprite(image);
            //Sprite a = new Sprite(image);
            Random randPos   = new Random();
            
            //Random randPosY   = new Random();
            aniBubbles[i].setLocation(randPos.nextInt(2200) + 40, randPos.nextInt(1500) + 40);
            
            //aniBubbles[i].setLocation(randPosX.nextInt(), randPosY.nextInt());
            sGroup2.add(aniBubbles[i]);
            //sGroup2.add(a);
        }
    }